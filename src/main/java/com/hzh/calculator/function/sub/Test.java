package com.hzh.calculator.function.sub;

import java.util.Optional;

public class Test {
/*    public static boolean isSameProvince2(User user1, User user2) {
        return Optional.ofNullable(getUserConsigneeProvince(user1))
                .map(p -> Optional.ofNullable(getUserConsigneeProvince(user2)).filter(p::equals))
                .isPresent();
    }

    public static String getUserConsigneeProvince(User user) {
        return Optional.ofNullable(user).map(User::getAddr).map(Addr::getProvince).orElse(null);
    }

    class User {
        Addr addr;

        public Addr getAddr() {
            return addr;
        }

    }

    class Addr {
        String province;

        public String getProvince() {

            return province;
        }
    }*/

}
